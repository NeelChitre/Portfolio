import { defineGlobalStyles } from '@pandacss/dev';

export const globalCss = defineGlobalStyles({
    body: {
        background: 'black',
        color: 'white',
        lineHeight: '1.2',
        fontFamily: 'poppins',
        overflowX: 'hidden'
    }
});
